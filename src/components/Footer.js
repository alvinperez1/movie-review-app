import React, { Component } from 'react'

export default class Footer extends Component {
  render() {
    return (
        <nav className="navbar sticky-bottom bg-danger">
        <div className="container-fluid">
        <span className="navbar-brand mx-auto text-white mb-0 fs-5">Make sure to leave a review!</span>
        </div>
      </nav>
    )
  }
}
